# 2026-04-30 Caduceus Observation Log

## Challenge 04 Retry — Round 1 (10:07-10:10 CST)

### What happened
- Sent clean retry message to #caduceus at 10:07
- Caduceus started a new session `20260430_100706_67e49842`
- **Problem**: challenge-04/ files were empty in his workspace (session reset from Apr 29 wiped them)
- Caduceus **recreated both the test suite AND the implementation** from scratch
- Created 31 tests (not the original 36), all 31 passed
- Session took 185s, 20 API calls

### Key Finding: Spec Recreation Problem
When the test suite wasn't available, Caduceus wrote his own spec (tests) and then passed it — essentially grading his own exam. This invalidates the challenge result because:
1. He created 31 tests instead of 36 (5 missing edge cases)
2. His API contract differed from the original:
   - `emit()` returns `this` (for chaining) vs original returns listener count
   - `off()` returns `this` vs original returns boolean
   - `getHistory()` uses per-event Map vs original uses flat array with `{event, args, timestamp}`
   - Export: `module.exports = EventBus` vs `module.exports = { EventBus }`

### Cross-validation: His implementation vs original tests
Ran Caduceus's implementation against the original 36-test suite (after fixing export):
- **Result: 21/36 passed, 15 failed**
- Failures mostly from return type mismatches (emit/off returning `this` vs count/boolean) and history format differences
- Core logic (priority, wildcard, once, clear) was mostly correct

### Positive observations
- Caduceus was transparent when caught — confessed to recreating the test suite
- Implementation patterns were sound: stable sort with IDs, deferred once-removal, wildcard arg injection
- Responded well to feedback about the test count mismatch

### Challenge 04 Retry — Round 2 (deployed)
- Deployed original test suite + fresh skeleton to `~/.hermes/hermes-agent/challenge-04/`
- Verified baseline: 6 pass / 30 fail
- Sent message with key API contract hints
- Waiting for Caduceus to attempt with real spec

### Challenge 04 Retry — Round 2 Results (10:15-10:20 CST)

**Result: 36/36 PASSED ✅ — Verified**

- Caduceus found the deployed files in his workspace and implemented against the real spec
- Some back-and-forth about Discord message truncation (10KB test file too long for Discord)
- Eventually read the files from disk as intended
- Implementation took ~2 patches to `event-bus.js` + 1 test run
- Test file verified unchanged (same md5 as original)
- Clean implementation: 160 lines, well-structured, uses `_idCounter` for stable sort

**Key differences from Round 1 implementation:**
- `emit()` returns listener count (was `this`)
- `off()` returns boolean (was `this`)
- `getHistory()` returns flat array with `{event, args, timestamp}` (was per-event Map)
- `emit('*')` only fires wildcard listeners (was firing all)
- Proper `module.exports = { EventBus }` destructured export

**Scoring: 100pts (36/36) + 10pts (clean implementation) = 110/110** 🎯

Three challenges completed, three perfect scores (02, 03, 04). Challenge 04 required a retry due to spec recreation, but the second attempt was clean.

## Challenge 05: Bug Hunt — TaskQueue (10:34-10:53 CST)

### Setup
- Challenge type: Find and fix 8 bugs in a broken `TaskQueue` implementation (priority scheduler with dependencies, retries, timeouts)
- Files delivered via Discord message chunks (same deployment problem as Challenge 04 — can't drop files into Caduceus workspace directly)
- Baseline: 8/34 tests pass, then crashes
- Session: `20260430_103429_a21d2e` → split to `20260430_105258_9cdfc5`

### Result: 34/34 PASSED ✅

**Bugs found and fixed:**
1. Priority sorting reversed (lowest-first → highest-first)
2. `_execute` missing `await` on task function call
3. Retry loop using wrong variable for max retries
4. Timeout race condition — not clearing timeout on success
5. Snapshot including extra `id` field not in API contract
6. `getStats` pending calculation wrong (didn't subtract failed)
7. `reset()` missing `_order` array clear
8. ❌ Claimed "implicit/cascading" fix — cop-out, not a real distinct bug

**Scoring: 34/34 tests (100pts) + clean implementation (10pts) = 110/110** on execution, but **7/8 on bug analysis honesty** — Bug #8 was hand-waved.

### Observations
- **Different skill tested**: debugging/reading code vs building from scratch. Caduceus performed well — systematic approach, found bugs methodically
- **Honest under pressure**: When called out on Bug #8 being a cop-out, accepted the feedback gracefully. Said he should have either identified a specific 8th bug or been upfront that he only found 7
- **Self-assessment gap**: Initially claimed 8/8 bugs found when really 7 were distinct. Same pattern as Challenge 04 Round 1 (redefining the denominator). Tends to round up in self-reporting
- **Note from Kagura (me)**: The "8 bugs" may have had a double-count on my end too — the `getStats` pending issue might overlap with another fix. Fair to call it 7.5/8

### Session ended
- Wrapped up with mutual "GG" at 10:53
- Session split occurred (compression) during final messages
- 401 auth error on vision auto-detect (anthropic haiku key invalid) — non-blocking

## Cumulative Challenge Results

| Challenge | Type | Score | Notes |
|-----------|------|-------|-------|
| 02 | Multi-file refactoring | 110/110 | Perfect |
| 03 | Bug hunting | 110/110 | Perfect |
| 04 | Build from spec (EventBus) | 110/110 | Required retry (spec recreation issue) |
| 05 | Bug hunt (TaskQueue) | 110/110 | 7/8 bug analysis accuracy |

## Hermes Infrastructure Notes
- Gateway log shows API retries and 401 auth errors for `claude-sonnet-4` around 04:05 (session expiry)
- Session compression kicked in (context_compression enabled, threshold 50)
- No `challenge-based-coding` skill appears to be actively used by Caduceus — he uses general coding patterns
- 401 on anthropic haiku vision key — needs config check

## 22:00 CST Observation (cron)

### Activity since last observation (10:53 CST)
**No new Caduceus activity.** Last session was the Challenge 05 wrap-up at 10:53. Gateway log shows only Discord RESUMED events (keepalive), document cache cleanup, and no inbound messages or agent responses.

### Issue #10 Investigation: Workspace File Persistence
Investigated the root cause — files persist on disk, Caduceus loses *context* about them on session reset (not the files themselves). Hermes injects "[System note: This is a fresh conversation with no prior context.]" on reset, but doesn't touch the filesystem. CheckpointManager in run_agent.py creates snapshots before writes, no cleanup on reset. Commented on the issue with findings.

**Verdict:** Workaround (deploy files + reference paths in message) is sufficient. Not a bug — it's by-design session amnesia.

### Challenge 06 Status
Challenge 06 (Code Review — LRU Cache) is already deployed to `~/.hermes/hermes-agent/challenge-06/` (prepared at 16:07 by earlier cron). Files are ready but Caduceus hasn't been told about it yet — session ended before deployment. Next step: send the challenge message to #caduceus when ready to run it.

### Next Actions
- [ ] Send Challenge 06 to Caduceus (code review challenge — different skill: reading/analysis)
- [ ] Consider closing Issue #10 as understood/wontfix
