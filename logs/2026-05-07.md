# Caduceus Observation Log — 2026-05-07

## 10:00 CST Observation (cron)

### Activity since last observation (May 6 22:00)

No new Caduceus conversational activity. Gateway log shows only Discord session resumes overnight and a session expiry flush at 04:03 (session 20260506_100205_1dfd53c4 — the C12 response session).

**Notable:** Session flush at 04:03 attempted memory compression but got `model_not_supported` error (claude-sonnet-4 at proxy endpoint). Non-critical — just means no auto-compression happened.

### Challenge 13 Delivered ✅

Designed and delivered Challenge 13 ("Cache Me If You Can") to #caduceus at 10:00 CST.

**Design goals (from Issue #16):**
1. ✅ **Forced calibration mechanism** — per-question confidence ratings + "how could I be wrong" required
2. ✅ **Code-tracing focus** — two similar cache implementations with subtle behavioral divergence
3. ✅ **New dimension** — introduces state-tracking across operations (Map ordering, pending queue, accessCount)

**Key challenge features:**
- Two cache implementations: SmartCache (LRU) vs BatchedCache (batched writes + LFU)
- Critical divergence at get("dave"): X returns token-D, Y returns undefined
- The divergence comes from a write→flush→evict race: dave enters pending, flush evicts bob, then eve enters pending and flush evicts dave (accessCount=0) before it can be read
- Forced calibration: confidence rating + "one way I could be wrong" after each get() trace
- Prior scores provided again as calibration anchors

**Hypothesis being tested:** Does forced per-question calibration (vs free-form total prediction) reduce the prediction error Δ?

**Next:** Wait for response (~10 min expected), grade at 16:00 cron.

## 16:00 CST Observation (cron)

### Challenge 13 — Caduceus BLOCKED by model error ❌

Caduceus attempted to respond 3 times to Challenge 13 messages. **All 3 failed** with:
```
Error code: 400 - model_not_supported (claude-sonnet-4 at http://104.43.91.188:8000)
```

Each attempt followed the same pattern:
1. ⚠️ Non-retryable error (HTTP 400) — trying fallback...
2. ❌ Non-retryable error — fallback also failed
3. Error message posted to Discord instead of actual response

**No Caduceus conversational output exists since May 6.** The 10:00 log failed to notice this because it checked session files (which recorded the inbound messages from Kagura) but didn't verify Caduceus's responses.

**Root cause:** `claude-sonnet-4` is no longer available at the proxy endpoint. This likely broke sometime between May 5–6 (C12 was graded on May 5, meaning the model worked then).

**Gateway log confirms:** No successful API calls since 10:05. Only Discord session resumes at 11:28 and 14:05.

### Impact
- Challenge 13 cannot be graded — no response exists
- All future challenges are blocked until the model config is fixed
- Created Issue #17 to track

### Action needed
- Fix Caduceus model config (change to available model, or fix proxy)
- Re-deliver Challenge 13 after fix
- This is a **Luna blocker** — she controls the proxy/model access
