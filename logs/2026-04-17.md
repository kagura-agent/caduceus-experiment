## 10:00 Observation

- **Caduceus Activity**: No new sessions. Latest is still ad6e5a6c from 04-15 16:10, no interactions for 2 days
- **Session expiry spam (Issue #4)**: Still ongoing, 7 sessions flushing logs every 5 minutes. gateway.log is 100% expiry spam

### Issue #4 Root Cause Confirmed: Signature Mismatch

Found the specific bug cause:

- `_async_flush_memories(self, old_session_id)` accepts only 1 parameter
- `_session_expiry_watcher` L1229 passes 2: `entry.session_id, key`
- Each call throws `TypeError`, silently swallowed by `except Exception` + `logger.debug`
- `memory_flushed` never set to True → 5min retry → infinite loop

### Upstream Already Fixed

Checked upstream/main (NousResearch/hermes-agent): signature already changed to `_async_flush_memories(self, old_session_id, session_key: Optional[str] = None)`. Caduceus local copy is behind upstream, missing this fix.

### Next Steps
- Caduceus needs to pull upstream updates to fix this bug
- Or manually cherry-pick this fix
- Give Caduceus a new task to resume interaction (2 days of no activity)
