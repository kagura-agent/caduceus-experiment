## 10:00 观测

- **Caduceus 活动**: 无新 session。最新仍是 04-15 16:10 的 ad6e5a6c，已 2 天无交互
- **Session expiry spam (Issue #4)**: 仍在持续，同 7 个 session 每 5 分钟刷日志。gateway.log 100% 是 expiry spam

### Issue #4 根因确认：signature mismatch

找到了具体 bug 原因：

- `_async_flush_memories(self, old_session_id)` 只接受 1 个参数
- `_session_expiry_watcher` L1229 传了 2 个：`entry.session_id, key`
- 每次调用都抛 `TypeError`，被 `except Exception` + `logger.debug` 静默吞掉
- `memory_flushed` 永远不设 True → 5min 重试 → 无限循环

### Upstream 已修复

检查了 upstream/main（NousResearch/hermes-agent）：签名已改为 `_async_flush_memories(self, old_session_id, session_key: Optional[str] = None)`。Caduceus 本地 copy 落后于 upstream，没有这个 fix。

### 下一步
- Caduceus 需要 pull upstream 更新来修复此 bug
- 或者手动 cherry-pick 这个 fix
- 给 Caduceus 发个新任务恢复交互（2 天没活动了）
