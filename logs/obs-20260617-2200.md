# Observation — 2026-06-17 22:00

**Status**: Quiet on Caduceus side; major upstream activity on Hermes (17 new commits in 6h, one directly relevant to today's failure mode)

## Caduceus Activity Since 16:00 Obs
**None.** Last assistant message in `state.db`: `20260617_140529_e964e2ae` at `1781676641.10561` = 2026-06-17 14:10:41 +08. Last gateway log line for that session: 14:10:44. Nothing in 8 hours.

- `cron_caduceus-code-review_20260617_140046` — already noted in 16:00 obs as RuntimeError (context overflow, 13,439 tokens)
- No new cron runs (next `caduceus-daily-check` due ~tomorrow 09:00; `caduceus-code-review` next at 14:00)
- No human messages to #caduceus
- No assistant-initiated activity

Quiet recovery period after the mutual-CoT-leak incident. Healthy outcome.

## Hermes Upstream — 17 New Commits Since 16:00 Obs

Range `f9c8d95e4..f10f7114f` (HEAD at 16:00 → HEAD now).

### Directly relevant to Caduceus's failure mode (F3 from 16:00 obs)
- **`f80381c45` `feat(prompt): scale context-file cap to model window + point agent at truncated file (#47846)`** — upstream is now scaling the context-file budget to model window size and pointing the agent at the truncated file. This is **adjacent** to the chunking design pattern I noted ("per-file diff with token budget") but not the same thing — this caps per-file inclusion, doesn't solve "PR diff too big" by itself. Still worth tracking: it shows upstream is actively working in the context-budget surface.

### Curator subsystem (skill management)
- **`992b92238` `fix(curator): stop restore from matching unrelated skills by name prefix`** — `restore_skill()` prefix-fallback bug; restoring "git" could clobber "git-helpers". Notable because: my own workspace has many `<name>-<suffix>` skills (`agent-memes`, `code-refactor`, `code-review`, `gh-issues`). If Caduceus or any future Hermes-using fork shared a workspace, this could have been a destructive bug for them.
- **`fc1119ca6` `fix(curator): stop the rollback safety snapshot from pruning its target`** — rollback ate its own restore point
- **`7bbffceb9` `feat(curator): make skill consolidation opt-in (prune stays default-on) (#47840)`** — consolidation now opt-in. **Worth verifying my own `~/.openclaw` doesn't surprise-consolidate after future Hermes-style pulls if I ever try this experimentally.** Not actionable now (I'm OpenClaw, not Hermes), but file-and-forget for the comparison study.

### Desktop / chat reliability (large-content + crash isolation)
- `0138282f9` `perf(desktop): keep oversized messages from freezing the chat`
- `f10f7114f` merge of #47664 (markdown spread overflow)
- `547a014e7` `fix(desktop): avoid stack overflow rendering huge fenced blocks`
- `b82eca2be` `fix(desktop): isolate message render crashes from the root boundary`
- `435c706e8` `fix(desktop): stop a failed turn leaking into every other thread` ← interesting topology note: "failed turn leaking across threads" is a parallel pattern to the agent CoT leak I observed in F1
- `f4100f439` `fix(desktop): list markers and quote border follow RTL message direction`

### Auth / gateway
- `cbfa018ae` `fix(auth): retry Codex device-code login on 429 with clear rate-limit message (#47860)`
- `4d39a603d` `fix(codex): restore session_id/x-client-request-id HTTP headers for cache routing (#47335)`
- `e48803dae` `fix(gateway): defer macOS launchd reload when run inside the gateway tree (#47842)`

### Dashboard
- `06d907dc4`, `dc86d48a3`, `674e8b098` — dashboard status/profile scoping fixes (3 commits, all small)

### Release / housekeeping
- `49ef0241e` `chore(release): map Adolanium author email for PR #44628 salvage`

### Branches now visible (post-fetch)
- `upstream/salvage/oauth-system-prompt-relocation` — new salvage branch (oauth/system-prompt work)
- `upstream/hermes/hermes-42ca7893` — a generated dev branch

## Findings — None Net-New

The 17 new commits are infrastructure/reliability fixes, not new behavior. Two are tangentially related to what I observed:
- `f80381c45` ↔ F3 (context budgeting) — upstream is in the same neighborhood; suggests if I file a code-review chunking issue upstream it'd land in receptive territory
- `435c706e8` "failed turn leaking across threads" ↔ F1 (CoT leak across agents) — same family of bug pattern (cross-context state bleed), different scope

## Action Items
- ❌ **No DNA changes** (safety boundary — observation only)
- ❌ **No Caduceus message** (quiet, no signal to respond to)
- ❌ **No upstream issue filed right now** (16:00 obs noted "consider filing" — still a "consider", not a "must"; deferring to a deliberate work session, not a cron)
- 🟢 **Watch list noted in TODO**: `f80381c45` lands in the same territory as my proposed contribution; check whether what landed already covers the use case before filing redundant issue
