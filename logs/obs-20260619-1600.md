# Observation — 2026-06-19 16:00

**Status**: Code-review cron failed again (F3 consecutive). Daily-check delivered but no session created. No new upstream release.

## Caduceus Activity Since Last Obs (2026-06-19 10:00)

### Code-review cron (14:00) — F3: context length exceeded

Same failure pattern as previous two days:
1. Browser tool timed out twice (60s each) trying to navigate GitHub
2. Fell back to web_search (two searches, both succeeded)
3. Then web_extract on `github.com/NousResearch/hermes-agent/pulls` — processed 23,994→5,052 chars
4. Hit context length limit: **18,228 tokens > 12,288 limit** on VM2 Copilot Gateway proxy
5. Compression attempted via claude-haiku-4-5 auxiliary → still exceeded → RuntimeError
6. Error message delivered to Discord #caduceus channel

```
agent.log: 2026-06-19 14:05:02 ERROR Context length exceeded (18,228 tokens). Cannot compress further.
agent.log: 2026-06-19 14:05:02 ERROR Job 'Daily Code Review' failed: RuntimeError
agent.log: 2026-06-19 14:05:03 INFO Job 'caduceus-code-review': delivered to discord
```

**Additional detail this run**: Copilot auth warnings visible — "Token from `gh auth token` is a classic PAT (ghp_*)" not supported by Copilot API. Caduceus is using auxiliary api-key fallback (claude-haiku-4-5) instead.

### Daily-check cron (09:00) — delivery only, no session

Same as previous days:
```
agent.log: 2026-06-19 09:00:58 INFO Job 'caduceus-daily-check': delivered to discord
```
No new session file created. Session persistence gap continues (last session file: June 12).

### Discord gateway — stable

RESUMED session at 14:33 (normal heartbeat). No invalidations. Connected as Caduceus#8719.

### Process health

Gateway PID 1666, started June 12, uptime ~7 days. Memory stable.
Browser session cleanup at 14:08 (inactive 324s), close timed out after 10s — minor leak.

## Hermes Upstream

Latest upstream/main commits (fetched):
- `3f0e9849e` refactor(tui): reuse DASHBOARD_TUI_MODE for hosted /exit guard
- `15e3b64b7` fix(tui): keep hosted dashboard chat alive on exit
- `3485bc722` Merge PR #48880 — Slack allowed-users validation
- `1ab6f3479` refactor(dashboard): align Slack allowlist validation
- `83c034bd5` fix(dashboard): accept Slack allow-all wildcard

All TUI/dashboard/Slack focused. No release beyond v2026.6.5.

## Persistent Issues (unchanged)

| Issue | Days | Root Cause |
|-------|------|------------|
| Code-review F2 → now F3 | 3+ | VM2 Copilot Gateway 12K prompt limit |
| No session files | 7+ | Session store likely broken/disabled since Jun 12 |
| Discord 100-cmd limit | 7+ | Slash command cap exceeded |
| Copilot PAT rejected | ongoing | Classic PAT not supported, falls back to api-key |

## Analysis

The code-review cron is structurally broken — browser timeouts inflate context, then web_search results push past the 12K limit. Three consecutive days of the identical failure. This won't self-resolve without either:
1. Raising VM2's prompt limit, or
2. Restructuring the cron to use fewer/smaller tool calls, or
3. Switching to a different model endpoint without the 12K cap

The browser timeout (2× 60s each run) adds ~200 tokens of error messages plus wastes 2 minutes, then web tools add the actual bulk. The real bottleneck is the proxy limit.

## Action Items
- ❌ **No DNA changes** (safety boundary)
- 📊 **F3 confirmed** — code-review cron needs intervention, not more observation
- 🟡 **Session persistence**: 7 days without session files. Upstream fix `1d2e35967` (session store warning) might help diagnose if Caduceus updates
- 🔴 **Recommendation**: Create GitHub issue on caduceus-experiment repo for the VM2 prompt limit blocker — 3 days of F2/F3 is enough data
